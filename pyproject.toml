[project]
name = "electronics-library"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "David Walker", email = "dbwalker0min@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "cyclopts>=4.10.1",
]

[project.scripts]
kicad-lib = "electronics_library:main"
electronics-library = "electronics_library:main"

[build-system]
requires = ["uv_build>=0.11.2,<0.12.0"]
build-backend = "uv_build"
